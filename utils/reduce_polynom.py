from utils.log import Logger


def reduce(before, after):
    logger = Logger("Reduce", True)
    logger.debug("Polylist1 = ", before)
    logger.debug("Polylist2 = ", after)
    for i, index in enumerate(before):
    eq = 0
    return eq