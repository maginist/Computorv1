from utils.log import Logger
from utils.polynom import Polynom


def get_real(before, after):
    real = 0
    floater = 0
    sign = 0
    
    return real


def reduce(before, after, vb, debug):
    logger = Logger("Reduce", debug)
    logger.debug("Polylist1 = ", before)
    logger.debug("Polylist2 = ", after)
    real = get_real(before, after)
    eq = 0
    return eq